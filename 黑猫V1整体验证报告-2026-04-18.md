# 黑猫V1 整体验证报告

日期：2026-04-18

## 结论

当前这套程序可以正常打开、登录和使用，核心链路没有发现新的阻断问题。

这轮整体验证里，实际确认并修正了 5 类问题：

1. 系统设置相关的外部接口检查，存在响应没有及时释放的情况，运行日志里已经出现过真实告警。
2. Telegram 通知请求虽然多数情况下会手动关闭，但遇到中途异常时仍可能留下未释放连接。
3. Docker 部署文件仍允许弱默认密钥直接启动。
4. compose 文件还保留了过时写法，并且默认 MySQL 版本高于当前校验链路的稳定范围。
5. 前端 `api` 和 `websocket` 之间存在相互牵扯，打包时会持续出现一条无效拆分提示。

## 已修复内容

### 1. 响应释放问题

- `ApiHealthCheckService.kt`
  - API 健康检查请求改为使用后立即关闭。
- `ProxyConfigService.kt`
  - 代理检测请求改为使用后立即关闭。
- `RetrofitFactory.kt`
  - RPC 节点校验请求改为使用后立即关闭。
- `TelegramNotificationService.kt`
  - 获取 Chat ID 和发送消息两条请求都改成强制收口，不再依赖手动关闭。

### 2. 回归检查补充

- `ApiHealthCheckServiceTest.kt`
  - 新增健康检查成功后必须关闭响应的检查。
- `RetrofitFactoryImplementationTest.kt`
  - 新增 RPC 校验必须关闭响应的检查。
- `TelegramNotificationServiceImplementationTest.kt`
  - 新增 Telegram 请求必须使用安全收口方式的检查。

### 3. 部署配置收紧

- `docker-compose.yml`
  - `DB_PASSWORD`、`JWT_SECRET`、`ENCRYPTION_KEY`、`ADMIN_RESET_PASSWORD_KEY` 已改为必填。
- `docker-compose.prod.yml`
  - 同步改为必填。
- `docker-compose.yml`、`docker-compose.prod.yml`、`docker-compose.test.yml`
  - 去掉过时的 `version` 头。
  - MySQL 镜像从 `8.2` 调整为 `8.1`，后续新部署不再自带这条兼容提示。

### 4. 前端依赖关系整理

- 新增 `authSession.ts`
  - 把登录失效后的断开逻辑拆成单独通道。
- `api.ts`
  - 不再动态加载 `websocket`。
- `websocket.ts`
  - 改为通过新的会话通知模块接收登录失效事件。
  - 打包时那条“同一模块既动态又静态加载”的提示已经消失。

## 验证结果

### 后端

- 定向测试通过：
  - `ApiHealthCheckServiceTest`
  - `RetrofitFactoryImplementationTest`
  - `TelegramNotificationServiceImplementationTest`
- `gradlew build` 通过
- 后端已按最新构建重新启动

### 前端

- `npm test` 通过
  - 5 组文件、18 条测试全部通过
- `npm run build` 通过

### 运行验证

- 页面可直接打开：
  - `/system-settings/notification`
  - `/system-settings/api-health`
- 使用账号 `990816 / 123456` 登录正常
- 接口返回正常：
  - API 健康检查：正常返回 10 项状态
  - 通知配置列表：正常
  - Leader 列表：当前 4 条

### 日志复验

- 修复前日志里存在多次连接泄漏告警：
  - `https://api.binance.com/`
  - `https://data-api.polymarket.com/`
  - `https://clob.polymarket.com/`
- 修复后重新触发健康检查，没有再出现新的同类告警。

## 还剩下的建议

1. 前端现在只剩一类打包提醒：部分页面包体积偏大。这个不影响使用，但后续可以把统计页、公告页继续拆小。
2. 测试时仍会看到一条 Vite 插件自身的旧配置提示，这不是业务故障，后续如果要清掉，需要单独升级前端插件栈。
3. 当前本机已经在运行的 MySQL 还是旧的 `8.2` 容器，所以本轮重启后日志里仍会看到兼容提示。部署文件已经修正；如果要把本机这条提示也去掉，需要在保留数据的前提下重建本地数据库容器。

## 交付

- 当前源码目录已更新这份最终报告。
- 最新源码已同步到桌面交付目录，便于直接检查。
