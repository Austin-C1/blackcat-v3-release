# 黑猫V1 消息推送页面修复报告

日期：2026-04-18

## 结论

消息推送页面这次实际有两层问题：

1. 前端把后台已经返回的中文失败原因吞掉了，只剩一层 `400`。
2. 部分测试动作在当前条件根本不满足时还允许点击，导致用户一按就报错。

另外还查到一条后台联动问题：

3. 机器人列表里的“测试”原本没有真正绑定到当前这一条配置。

以上问题已修复，页面相关测试、前端打包、后端构建和浏览器实测均已通过。

## 实际改动

### 前端

- 增加统一的接口报错提取逻辑，优先显示后台返回的中文原因。
- 消息推送页和 Telegram 配置弹窗都改成显示真实失败原因，不再只提示 `Request failed with status code 400`。
- 机器人列表里的“测试”只会对当前这一条配置发送测试消息。
- 没有可用 Telegram 配置时，模板区测试按钮和配置行测试按钮会直接禁用，并显示原因提示。
- 三套语言文案补了“当前不可测试”的提示语。

### 后端

- 通知测试接口改为支持 `configId`，可以准确测试指定配置。
- Telegram 测试发送逻辑补上按配置 ID 选择目标配置的能力。
- 新增控制器测试，锁定“指定配置测试”这条行为。

## 原因

这页之前最容易误导人的地方是：

- 报错明明有具体原因，但页面只给你一个空泛的 `400`。
- 某些按钮在没有可用配置时仍然开放，用户会误以为是功能坏了。
- 列表行里的“测试”看起来像测这一行，实际上不是。

## 验证结果

- 前端 `8` 组、`24` 条测试通过。
- 前端 `npm run build` 通过。
- 后端 `gradlew build` 通过。
- 浏览器实测通过：
  - 机器人配置页正常打开。
  - 配置行“测试”按钮可正常发送测试消息。
  - 模板区“测试”按钮可正常发送测试消息。
  - 失败时会显示具体中文原因，不再只显示 `400`。

## 运行状态

- 前端：`http://127.0.0.1:18880`
- 后端：`http://127.0.0.1:8000`
- 当前 Telegram 配置：`查理`

## 补充修复

- 单条机器人配置的“测试”按钮原来走的是另一句已经损坏的固定文案，所以 Telegram 里会看到乱码。
- 页面现在已经统一成同一条正常中文测试消息：`这是一条测试消息`。
- 对应前端检查已补上，避免之后又把单条测试改回乱码。
