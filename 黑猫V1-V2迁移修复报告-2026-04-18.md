# 黑猫V1 V2迁移修复报告（2026-04-18）

## 结论
这次修的是 Polymarket V2 迁移会直接影响下单的部分。旧版 V1 的下单结构、交易所地址、抵押品授权和账户初始化逻辑已经切到 V2；后端构建通过，前端测试和打包通过。

## 实际改动
1. 下单签名改成 V2 结构，不再依赖旧版 nonce / feeRateBps，改为使用 timestamp / metadata / builder。
2. 交易所和抵押品常量改成 V2 地址，抵押品改为 pUSD。
3. 账户初始化检查和授权步骤改成 V2 规则：检查 pUSD 到 CTF 的授权，以及 CTF 到两个交易所的授权。
4. 底层链上查询和授权交易改成 V2：新增 pUSD approve 和 CTF setApprovalForAll。
5. 前端账户初始化展示改成识别 approved / unlimited / pUSD 文案。
6. 通知模板语言表里已移除旧的 CRYPTO_TAIL_SUCCESS 残留，相关词条恢复正常。
7. 补了 V2 相关后端测试，并修正了 Kotlin + Mockito 下测试自身的参数写法问题。

## 验证结果
- 后端定向测试通过。
- 后端 `gradlew build` 通过。
- 前端 `vitest` 5 组文件、18 条测试全部通过。
- 前端 `npm run build` 通过。

## 备注
- 前端构建仍有 Vite 插件旧配置提示，以及大包体积提醒，但这两项不影响当前功能和打包结果。
