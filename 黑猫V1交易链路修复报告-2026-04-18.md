# 黑猫V1 交易链路修复报告

日期：2026-04-18

## 这轮确认存在并已修复的问题

1. 风控统计每次下单前都会重复查库，已加 10 秒短缓存，并在新订单写入后主动清缓存。
2. 交易热路径缺少关键复合索引，已补 3 个索引。
3. 数据库连接池偏小，已调大到更适合并发跟单的配置。
4. 同一市场短时间内重复拉取 orderbook，已加 2 秒短缓存。
5. 卖出匹配原来按配置逐个查，已改成批量一次查完再分组使用。
6. FAK 买单提交成功后直接当成已成交，已改成先记为 `pending_fill`，确认成交后再转正式状态。
7. leader 快买快卖时，卖出查询可能先查不到对应买单，已加一次延迟重试。
8. 链上回退会把系统自己刚发出的卖单再次拿去做兜底匹配，已加最近卖出记录过滤。

## 这轮核查结论

- 上面 8 项问题都真实存在，不是误报。
- 交易检测层本身没有发现新的明显瓶颈，问题主要集中在下单前的数据库与状态流转。
- 这轮没有再把现有正常链路改坏；原先已经确认修好的并发、TLS、弹窗状态和通知作用域释放都保持正常。

## 重新验证结果

### 后端

- 交易链路相关定向测试通过。
- 后端完整构建通过。

### 前端

- 前端测试通过：5 组文件、18 条测试全部通过。
- 前端打包通过。

### 实际运行检查

- 登录接口可正常使用：`990816 / 123456`
- `http://127.0.0.1:18880/system-settings/notification` 直接打开返回 200，不再是 404。
- `http://127.0.0.1:18880/system-settings/api-health` 直接打开返回 200。
- 通知配置接口返回正常。
- API 健康检查接口返回正常，未配置项显示为“未配置/跳过”，不再误报成异常。
- Leader 列表接口返回正常，当前仍为 4 条。
- 跟单配置列表接口返回正常，当前为 0 条。

## 交付说明

- 本报告已放在源码目录。
- 最新代码与本报告会同步到桌面文件夹 `polymarket自动跟单` 里，供直接检查。
