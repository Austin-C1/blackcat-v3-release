# 黑猫V1 修复报告

日期：2026-04-18

## 这次确认并修好的内容

1. JWT 认证失败现在返回 401，不再返回 200。
2. `encryption.key` 不再回退到 `jwt.secret`。
3. `application.properties` 里去掉了弱默认值，启动必须显式提供数据库、JWT、加密和重置密码相关配置。
4. Docker / Nginx 不再按 root 方式跑前端容器入口，监听端口改为容器内 8080。
5. Docker 编排里去掉了 MySQL 对宿主机的默认暴露。
6. 前端剩余的废弃属性写法已经清理，`handleSubmit(...: any)` 这批也已经补了类型。
7. 前端打包过程中额外发现的空白页问题已修，原因是自定义拆包把启动链路拆坏；现在已改回稳定方案。
8. 你刚才截图里的 `18880/...` 直链 404 已处理，原因是当时 18880 跑的是不支持前端路由的静态服务；现在已经切回支持路由的前端服务。

## 验证结果

- 后端定向测试通过。
- 后端完整构建通过。
- 前端测试通过，5 组文件、18 条测试全部通过。
- 前端打包通过。
- 未登录访问受保护接口时，真实返回为 401。
- 使用账号 `990816 / 123456` 真实登录成功。
- 真实接口检查通过：
  - 账户列表正常返回。
  - Leader 列表正常返回，当前仍是 4 条。
  - 跟单配置列表正常返回。
  - 公告详情正常返回。
  - API 健康检查正常返回，`Polymarket Activity WebSocket`、`链上 WebSocket` 等未配置项显示为“未配置/跳过”。
- 页面侧确认：
  - 登录页可打开。
  - 账户页可打开。
  - Leader 页可打开。
  - `http://127.0.0.1:18880/system-settings/notification` 这类直链现在返回 200，不再掉到 404。

## 当前本地运行状态

- 前端：`http://127.0.0.1:18880`
- 后端：`http://127.0.0.1:8000`
- 本地 MySQL 容器：已启动并健康

## 交付说明

- 本报告会和最终代码一起放到桌面文件夹 `polymarket自动跟单` 中。
