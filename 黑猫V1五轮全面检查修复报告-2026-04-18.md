# 黑猫V1 五轮全面检查修复报告
日期：2026-04-18

## 结论

当前这套程序可以正常打开、登录、查看 Leader、账户、通知和系统设置。
前面五轮检查里确认并修掉的主要问题包括：

1. 认证失败、参数错误这类接口以前会表面返回成功状态，现在已经改成真实失败状态。
2. 后端旧日志链路里那类重复异常没有再继续新增。
3. Leader 页面以前把“没有跟单关系”误当成“不能查看资产”，现在已经恢复成始终可看。
4. Neg Risk 赎回和 WCOL 解包之前混用了不同地址，现在已经分开。
5. Leader 列表页的资产展示方式和真实使用习惯不一致，现在已经改正。

## 五轮检查结果

### 第 1 轮
- 检查重点：认证接口、后端启动日志
- 发现问题：
  - 登录、重置密码、WebSocket 票据这些失败时会返回错误内容，但 HTTP 状态还是 200
  - 旧日志里曾出现过 ThrowableProxy 相关异常
- 处理结果：
  - 认证接口失败状态已经改成正确状态码
  - 重新核对后，后续新请求没有再继续产出同类异常

### 第 2 轮
- 检查重点：系统设置、通知、代理、RPC 相关接口
- 发现问题：
  - 一批旧接口仍会把失败包成“表面成功”
- 处理结果：
  - 已增加统一状态修正
  - 这批旧接口不需要逐个重写，也会返回正确状态

### 第 3 轮
- 检查重点：Leader 页面真实用途
- 发现问题：
  - 页面把“未关联跟单”误当成“未监控”，直接不拉余额和仓位
- 处理结果：
  - 去掉这层限制
  - Leader 列表和详情恢复读取余额和仓位

### 第 4 轮
- 检查重点：Neg Risk 赎回和 WCOL 解包
- 发现问题：
  - Neg Risk 赎回目标和 WCOL 解包目标混用
  - 会导致赎回链路和后续解包链路对不上
- 处理结果：
  - Neg Risk Adapter 和 Wrapped Collateral 地址已经拆开
  - Neg Risk 赎回改成走 Adapter
  - WCOL 解包继续走 Wrapped Collateral

### 第 5 轮
- 检查重点：整包构建、运行服务、真实接口回归
- 发现结果：
  - 后端完整构建通过
  - 前端全部测试通过，打包通过
  - 后端重启后，Leader 列表、Leader 余额、认证失败状态、系统设置失败状态都符合预期
  - 新请求打过之后，旧异常日志没有继续增长

## 补充修正

- 这次又查到一个页面层问题：Leader 列表页的数据链路本身是对的，但余额请求是逐条排队的，页面刚打开时容易整列转圈；同时列表列名只写“可用余额”，而真实的大数通常在“持仓资产”。
- 处理结果：
  - Leader 列表页改成并行加载每个 Leader 的资产数据
  - 列表标题改成“资产概览”
  - 列表和移动端卡片都把“持仓资产”放到第一行，“可用余额”放到第二行
  - 对应前端规则检查已经补上
- 这次又核对了你给的截图，发现还有一层展示问题：页面虽然已经会取余额接口，但没有把“当前持仓”和“当前持仓价值”直接放在列表主区域，所以看起来仍然不像在看真实仓位。
- 处理结果：
  - Leader 列表桌面端直接显示“当前持仓价值”
  - Leader 列表桌面端和移动端都补上“当前持仓”摘要，显示持仓数量和前两条持仓预览
  - Leader 列表不再把“可用余额”放在主显示位置，改成“当前持仓金额”
  - 已确认 `DrPufferfish` 当前接口返回有 30 条持仓，持仓价值为 `236414.3526`
  - 你截图里 Polymarket 站内资料卡显示的总值与公开 Data API 当前返回值不一致，这部分属于上游展示口径或时间差，不是这个页面本地计算出来的错误值
- 又补查到一个真正会导致整列一直转圈的问题：Leader 列表的资产加载在请求发出后，会因为依赖项变化把这一轮结果提前取消，导致余额接口虽然已经返回，页面状态却一直落不到可显示结果。
- 处理结果：
  - 资产加载改成只在 Leader 列表变化时重新触发
  - 当前加载状态改为通过内部引用保存，不再把自己重新触发后又取消
  - 已重新验证四个 Leader 的余额接口都在 1 秒内返回，页面不再卡在整列转圈

## 实际改动

### 后端
- 认证接口失败状态修正
- 统一错误状态修正
- Neg Risk 赎回和 WCOL 解包地址分离
- 补了针对状态码、日志、赎回构造的测试

### 前端
- Leader 管理页恢复始终读取余额和仓位
- Leader 列表页改成并行加载资产
- Leader 列表改成以“持仓资产”为主显示
- 详情弹窗保留手动刷新
- 更新对应页面规则检查

## 验证结果

### 构建与测试
- 后端 `gradlew build` 通过
- 后端新增定向测试通过
- 前端 `vitest` 6 组文件、20 条测试全部通过
- 前端 `npm run build` 通过

### 接口实测
- 登录正常
- Leader 列表返回 4 条
- Leader 余额接口可直接取数，DrPufferfish、3crocodile3、leegunner、Tmao12345 的持仓数据都能正常返回
- 空用户名登录返回 400
- 自动赎回缺参数返回 400
- 代理删除缺参数返回 400

### 页面检查
- `http://127.0.0.1:18880/leaders` 可正常打开
- 当前 `18880` 跑的是这份源码目录下的最新前端
- Leader 列表页已经切到“资产概览”，并行请求生效
- Leader 列表页现在会直接显示“当前持仓价值”“当前持仓金额”“当前持仓”

## 还剩的非阻断提醒

1. 前端打包仍有包体积提醒，但不影响当前功能。
2. MySQL 8.2 的兼容提示仍会出现在启动日志里，因为当前本机数据库容器还是 8.2；这不是程序故障。
3. `monitoringEnabled` 这个字段目前仍保留在后端数据里，用来表示是否存在启用中的跟单关系，但不再用于阻止 Leader 页面查看资产。
